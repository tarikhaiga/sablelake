<!-- Hero -->
<div class="max-w-[85rem] mx-auto py-8 px-4 sm:px-6 lg:px-10">
  <!-- Grid -->
  <div class="grid lg:grid-cols-7 lg:gap-x-8 xl:gap-x-12 lg:items-center">
    <div class="lg:col-span-3">
      <h1 class="block text-3xl font-bold text-gray-800 sm:text-4xl md:text-5xl lg:text-6xl">Mobile Book</h1>
      <p class="mt-3 text-lg text-gray-800 ">Introducing a new way to use your pocket.</p>

      <!-- Form Start -->
      <form id="emailForm" class="relative">
        <div class="mt-5 lg:mt-8 flex flex-col items-center gap-2 sm:flex-row sm:gap-3">
          <div class="w-full sm:w-auto">
            <label for="hero-input" class="sr-only">Email</label>
            <input type="email" id="hero-input" name="hero-input" required class="py-3 px-4 block w-full xl:min-w-[18rem] border-gray-900 text-sm focus:border-blue-900 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none" placeholder="Enter work email">
            <!-- Error Message -->
            <!-- <span id="error-message" class="absolute text-red-500 text-sm hidden" style="top: 100%; left: 0; margin-top: 0.25rem;">Please enter your email to receive exclusive updates on our pocket book release.</span> -->
            <!-- Confirmation Message (Addition) -->
            <span id="confirmation-message" class="absolute text-green-700 text-sm hidden" style="top: 100%; left: 0; margin-top: 0.25rem;">
              <svg class="inline-block mr-1" width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M5.5 8L7.5 10L11.5 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              Email successfully submitted! You will receive a further update from us soon.
            </span>
          </div>
          <button type="submit" class="w-full sm:w-auto py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-semibold border border-transparent bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50 disabled:pointer-events-none">
            Reserve Your Book
          </button>
        </div>
      </form>
      <!-- End Form -->

      <!-- Brands -->
      <!-- ... existing content ... -->
      <!-- End Brands -->
    </div>
    <!-- End Col -->

    <div class="lg:col-span-4 mt-10 lg:mt-0">
      <img class="w-full" src="./public/landing-image.png" alt="Image Description">
    </div>
    <!-- End Col -->
  </div>
  <!-- End Grid -->
</div>
<!-- End Hero -->

<!-- JavaScript for Form Handling, Validation, Confirmation, and Clearing Input (Adjustment) -->
<script>
  document.getElementById('emailForm').addEventListener('submit', function(event) {
    event.preventDefault();
    var emailInput = document.getElementById('hero-input');
    var email = emailInput.value;
    // var errorMessage = document.getElementById('error-message');
    var confirmationMessage = document.getElementById('confirmation-message');

    if (email.trim() === '') {
      // errorMessage.classList.remove('hidden');
      confirmationMessage.classList.add('hidden');
    } else {
      // errorMessage.classList.add('hidden');
      confirmationMessage.classList.remove('hidden');
      console.log('Email submitted:', email);
      // Clear the input field
      emailInput.value = '';
      // Here you can add your code to handle the email (e.g., sending it to a server)
    }
  });
</script>
